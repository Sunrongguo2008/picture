name: Convert images to AVIF

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (no man-db)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends libavif-bin imagemagick jq
          sudo rm -rf /var/lib/apt/lists/*

      - name: Run conversion script
        run: |
          chmod +x scripts/convert_to_avif.sh
          ./scripts/convert_to_avif.sh

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ -n "$(git status --porcelain)" ]; then
            git add README.md
            git add . -- ':!node_modules' ':!.git' ':!.github'
            git commit -m "Add AVIF conversions"
            git push
          else
            echo "No changes to commit."
          fi
